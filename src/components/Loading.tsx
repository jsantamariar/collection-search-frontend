const Loading = () => {
  return (
    <div className="flex items-center justify-center h-screen bg-slate-50">
      <p className="text-center text-black text-2xl">Loading...</p>
    </div>
  );
};

export default Loading;
